import 'package:flutter/material.dart';

///
class NotificationScreen extends StatelessWidget {
  ///
  const NotificationScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Text('Notification screen'));
}
